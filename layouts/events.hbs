<!DOCTYPE html>
<html lang='en'>
    <head>
        {{> head }}
    </head>

    <body data-slug='{{#if path}}{{ path }}{{else}}home{{/if}}' class='off-canvas-contain'>
        {{> header }}

        <section class='main-body row'>
            <article class='main-content sm-12 md-12 lg-9' data-pagefind-body>
                <h2>Event Schedule</h2>

                <h3>Upcoming</h3>

                <ul class='upcoming-events'>
                    {{> events events=futureEvents}}
                </ul>

                <h3>Past</h3>

                <ul class='past-events'>
                    {{> events events=pastEvents}}
                </ul>

                <aside id='event-stats'>
                    <h3>Event Statistics</h3>
                    <p>
                        I have delivered {{ eventStats.totalTalks }} talks or sessions at {{ eventStats.totalEvents }} events. 
                        These events were in {{ eventStats.locationCount }} different locations with 
                        {{ eventStats.byLoc.Virtual }} events being virtual.
                    </p>
                    <p>These talks have covered many nuanced concepts, but the broad topics are below:</p>
                    <ul>
                        {{#each eventStats.byTopic}}
                            <li>{{@key}} ({{this}})</li>
                        {{/each}}
                    </ul>
                    <p>I have been giving conference talks since 2013, peaking in 2015-2016 as a professional Developer Advocate.</p>
                    <ul>
                        {{#each eventStats.byYear}}
                            <li>{{@key}}: ({{this}})</li>
                        {{/each}}
                    </ul>
                </aside>

            </article>

            {{> sidebar }}
        </section>

        {{> footer }}

        {{> scripts }}
    </body>
</html>
