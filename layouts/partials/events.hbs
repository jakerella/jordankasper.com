{{#each events}}
    <li>
        <span class='event-name'><a href='{{ url }}' title='Visit this event website'>{{ name }}</a></span>
        <span class='event-location'>{{ location }}</span>
        <span class='event-date'>{{ monthDate }}</span>
        <span class='event-topics'>
            {{#each topics as |topic i|}}{{#if i}}, {{/if}}{{ topic }}{{/each}}
        </span>
        {{#if media}}
            {{#each media}}
                <span class='event-slides'>[<a href='{{ href }}' title='{{ title }}'>{{ type }}</a>]</span>
            {{/each}}
        {{/if}}
    </li>
{{/each}}
